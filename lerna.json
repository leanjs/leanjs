{
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "version": {
      "ignoreChanges": ["**/*.md", "**/*.test.*", "e2e/**"]
    },
    "publish": {
      "ignoreChanges": ["**/*.md", "**/*.test.*", "e2e/**"],
      "conventionalCommits": true,
      "message": "chore: publish",
      "allowBranch": "main"
    }
  }
}
